{
  "name": "api",
  "version": "0.0.1",
  "main": "./main.js",
  "type": "module",
  "dependencies": {
    "bcrypt": "^6.0.0",
    "express": "^4.21.2",
    "jsonwebtoken": "^9.0.3",
    "mongoose": "^9.6.2",
    "zod": "^4.4.3"
  },
  "scripts": {
    "start": "node main.js",
    "build": "npx esbuild src/main.ts --bundle --platform=node --format=esm --outfile=dist/main.js --external:express --external:mongoose --external:bcrypt --external:jsonwebtoken --external:zod && cp package.json dist/ && cd dist && npm install --production"
  }
}
